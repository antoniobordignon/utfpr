#include <stdio.h>

int main(){
	float largura, altura, comprimento; 
	float volume;
	
	printf("informe largura, altura e comprimento: \n");
	scanf("%f" "%f" "%f", &largura, &altura, &comprimento);
	
	volume = largura * altura * comprimento;
	printf("volume = %f", volume);
}
